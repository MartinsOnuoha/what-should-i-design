# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    environment: 'production'
    env:
      VITE_APP_TITLE: ${{ vars.VITE_APP_TITLE }}
      VITE_APP_DESCRIPTION: ${{ vars.VITE_APP_DESCRIPTION }}
      VITE_APP_WEBSITE: ${{ vars.VITE_APP_WEBSITE }}
      VITE_APP_IMG: ${{ vars.VITE_APP_IMG }}
      VITE_APP_KEYWORDS: ${{ vars.VITE_APP_KEYWORDS }}
      VITE_APP_LANG: ${{ vars.VITE_APP_LANG }}
    runs-on: ubuntu-latest
    steps:
      - name: Check variables
        run: |
          echo "VITE_APP_TITLE: $VITE_APP_TITLE"
          echo "VITE_APP_DESCRIPTION: $VITE_APP_DESCRIPTION"
          echo "VITE_APP_WEBSITE: $VITE_APP_WEBSITE"
          echo "VITE_APP_IMG: $VITE_APP_IMG"
          echo "VITE_APP_KEYWORDS: $VITE_APP_KEYWORDS"
          echo "VITE_APP_LANG: $VITE_APP_LANG"
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_WHATSHOULDIDESIGN }}'
          channelId: live
          projectId: whatshouldidesign
